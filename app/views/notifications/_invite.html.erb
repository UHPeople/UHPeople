<tr>
  <td class="mdl-data-table__cell--non-numeric">
    <%= link_to notification.tricker_user.name, notification.tricker_user, class: 'alert-link' %>
  	has invited you to join
    <% if notification.tricker_hashtag != nil %>
      <%= link_to '#'+notification.tricker_hashtag.tag, hashtag_path(notification.tricker_hashtag.tag), class: 'alert-link' %>
    <% else %>
      <i>deleted channel</i>
    <% end %>

    <span class="timestamp"><%= notification.timestamp %></span>
      <td>
      <% unless current_user.hashtags.include? notification.tricker_hashtag %>
        <%= link_to 'Join', join_path(tag: notification.tricker_hashtag.tag), method: 'post', class: 'btn btn-default' %>
      <% else %>
        <i>Already a member</i>
      <% end %>
    </td>
  </td>
</tr>
