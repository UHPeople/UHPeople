<div class="feed-paragraf">
  <h2>Suuri Limputuskisa!</h2>

  <div class="top">
    <h3>Top Hashtags</h3>
    <ul>
      <% @top_hashtags.each do |top| %>
        <li><%= link_to "##{top[0].tag} - #{top[1]} messages", hashtag_path(top[0].tag) %></li>
      <% end %>
    </ul>
  </div>

  <div class="top">
    <h3>Top Users</h3>
    <ul>
      <% @top_users.each do |top| %>
        <li><%= link_to "#{top[0].name} - #{top[1]} messages", top[0] %></li>
      <% end %>
    </ul>
  </div>

  <p>Kilpailuaika: 14.-30.9.2015</p>

  <p>Fuksiryhmän, jonka kanavalla on keskusteltu eniten syyskuun loppuun mennessä, kesken jaetaan palkintoja.<br>
    Lisäksi koko sivustolla eniten keskustellut henkilö voittaa palkinnon.</p>

  <p>Tällä sivulla näkyvät aktiivisimmat fuksiryhmät ja kovimmat limputtajat. Lisäksi
    <a href="https://limppu.it.helsinki.fi/hashtags/limputuskisa">#limputuskisa-kanavalle.</a>.</p>
</div>
