<div class="col-md-3">
	<h2>My interests</h2>
	<table class="table table-hover">
		<% user.hashtags.each do |tag| %>
			<tr>
				<td>
						<a class="panel-title"><%= link_to "#"+tag.tag, tag %></a>
				</td>
			</tr>
		<% end %>
	</table>
</div>