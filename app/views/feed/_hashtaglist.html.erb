<div class="col-md-3">
  <h2>Interests</h2>
  <table class="table table-hover">
    <% @tags_in_list.each do |tag| %>
        <tr>
          <td>
            <a class="panel-title"><%= link_to "#"+tag.hashtag.tag, tag.hashtag %></a>
            <% if tag.favourite? %>
                <%= link_to "", {controller: "user_hashtags", action: "update", id: tag.id},
                            method: :put, class: "glyphicon glyphicon-star", style: "color:magenta" %>
            <% else %>
                <%= link_to "", {controller: "user_hashtags", action: "update", id: tag.id},
                            method: :put, class: "glyphicon glyphicon-star-empty", style: "color:magenta" %>
            <% end %>
          </td>
        </tr>
    <% end %>
  </table>
</div>
