<div id="masonry-container" class="transitions-enabled infinite-scroll clearfix col-xs-12">
  <div class="container">
    <% unless @chats.any? %>
      <p class="lead">
        You have no favourites selected. Star some interests to see something here!
      </p>
    <% else %>
      <% @chats.each do |chat| %>
        <%= render 'chatbox', chat: chat %>
      <% end %>
    <% end %>
  </div>
</div>
